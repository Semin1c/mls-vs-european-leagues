# Python
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Notes:
# - Data IS committed (small, public) so the repo runs out-of-the-box.
# - Generated figures ARE committed so results are browsable without running the code.

# Working screenshots and superseded figures stay on disk but out of the repo.
# The originals also live in the untouched project folder one level up.
archive/
